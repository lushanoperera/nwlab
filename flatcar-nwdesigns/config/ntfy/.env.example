# ntfy — flatcar-nwdesigns (VM 104)
#
# Optional admin token for locking down topics later. Leave unset while
# auth-default-access is "read-write" (v1 posture — LAN-only, trusted clients).
# NTFY_ADMIN_TOKEN=
