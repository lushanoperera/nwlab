# Caddy reverse proxy — all runtime config via Caddyfile + Docker secrets.
# Cloudflare API token lives in ./secrets/cloudflare_api_token (git-ignored).
# Timezone is hardcoded to Europe/Rome in docker-compose.yml to match the other
# flatcar-nwdesigns stacks.
